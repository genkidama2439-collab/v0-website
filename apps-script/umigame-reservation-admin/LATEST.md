# 最新のGAS変更

更新日: 2026-08-13

## 最新コード

現在の完全版：[`Code.gs`](Code.gs)

表示確認用：[`TestMail.gs`](TestMail.gs)

内容:

- 既存19列を変更せず、末尾にメール、顧客行動ID、同意記録、流入・端末情報、参加者数値、要望・アレルギー等を追加
- 既存予約を削除せずに列見出しを追加する`ensureBookingSchema_`を追加
- 管理者メールへ`合計受取金額`を表示（単体プラン・全セット共通）
- セットの売上内訳の末尾へ`合計受取金額`を追加（内訳各行の実額を合算）
- Googleカレンダーのセット各イベントへ【セット全体の受取金額】ブロックを追加
- 現金の受取担当を「その日の最初のツアー」に固定し、他イベントは受け取らない旨を表示
- 金額の計算ロジックは変更なし。表示のみの変更

本番反映状況:

- 顧客・行動連携列: **未反映（2026-08-13）**
- 管理者メール分: **反映済み（2026-08-01・ユーザー確認）**
- Googleカレンダー分: **未反映**

## 本番へ反映する手順

1. `Code.gs`を全選択コピーし、Apps Scriptエディタの`Code.gs`へ貼り付けて保存
2. `デプロイ` → `デプロイを管理` → 対象のウェブアプリを編集（鉛筆）
3. バージョンを`新バージョン`に変更して`デプロイ`（`/exec`のURLは変わらない）
4. サイトからテスト予約を1件入れ、管理者メールとカレンダーの各予定を確認
5. 確認後、テスト予約の予定を削除

`doPost`はデプロイ済みバージョンのコードで動くため、手順3を省くと反映されません。

## 管理状態

- `Code.gs`：合計受取金額の表示を含む最新版
- `TestMail.gs`：`testReceiveTotalEmail`で管理者メールの表示を確認する補助ファイル。本番動作には不要で、削除しても問題なし
- `LineSafeSend.gs`：LINE安全送信の変更部分（`Code.gs`へ統合済み）
- `Code.next.gs`：現在はなし
- 反映前の旧版：`archive/Code.2026-07-16.before-line-safe.gs`
